def num_one_bit(n):
    count = 0
    while n != 0:
        if n & 1 == 1:
            count += 1
        n >>= 1
    return count

def num_one_bit_optimal(n):
    count = 0
    while n !=0:
        count += 1
        n = n & (n-1) # eliminate the right most set bit
    return count


